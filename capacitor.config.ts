import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tuempresa.myapp',
  appName: 'Love Tester',
  webDir: 'dist/myapp/browser',
};

export default config;
